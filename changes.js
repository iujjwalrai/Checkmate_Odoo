// changes.js
// Created on mobile for urgent commit

// ✅ Sample function (replace with your logic)
function greetUser(name) {
  return `Hello, ${name}!`;
}

// ✅ Sample constant
const today = new Date();
console.log("Today's date is:", today.toDateString());

// ✅ Sample object change
const user = {
  id: 1,
  name: "Tanveer Singh",
  status: "Active"
};

console.log(greetUser(user.name));
